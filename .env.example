# Clave pública de TMDB usada por Vite para consultar el catálogo.
VITE_TMDB_API_KEY=tu_clave_de_tmdb

# Configúrala únicamente en Vercel (Settings > Environment Variables).
# Nunca uses el prefijo VITE_ para este valor.
FILMIFY_ACCESS_CODE=un-codigo-largo-y-privado
